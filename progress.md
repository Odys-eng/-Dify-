# 分析进度

## 2026-07-21

- 已建立只读对比计划。
- 下一步：盘点新旧项目目录与版本标识。
- 二级目录全量盘点超时，已切换为分层和定向检索。
- 已完成顶层结构对比：旧版为完整源码仓库，当前项目为部署/业务交付结构。
- 已从 README 确认当前目标版本为 Dify 1.16.0，并锁定需比较的业务资产。
- 已盘点双方自有资产，确认知识库内容、提示词和独立文档工具的复用价值最高。
- 已检查 DSL、导入报告、Compose 和旧版 Dataset API 示例，发现 DSL 仍有实例绑定项及一个联网分支逻辑问题。
- 已确认当前 Dify 为 1.16.0，并发现旧版主要运行状态保存在 Docker 数据卷内。
- 已确认旧版底层为 Dify 1.15.0，版本跨度仅为 1.15 -> 1.16。
- 已确认新旧实例各有独立数据卷，且旧用户应用没有落盘的 DSL 导出。
- 已比较环境变量、插件和加密密钥，确认整库升级与资源级合并需要两条不同路线。
- 已发现当前上传脚本硬编码 API Key 和自动选择首个知识库的风险。
- 已完成复用矩阵、两条迁移路线及推荐执行顺序。
- 首次运行计划完成检查时被本机 PowerShell 执行策略阻止，已记录并改用进程级临时 Bypass。
- 开始新任务：启动 Dify，对比本地文件与线上文档并修复错误索引。
- `startup.ps1` 因 UTF-8 无 BOM 与 Windows PowerShell 5 编码不兼容而解析失败，切换为直接启动 Compose。
- `docker compose up -d` 已成功；HTTP/API 尚不可达，正在拆分检查容器状态与日志。
- 已确认 API 容器内部健康（1.16.0 / HTTP 200）；改用容器内部 API 执行知识库核对。
- 已完成本地 102 文件与线上 23 文档核对：缺失 79，错误 2，正常 21。
- 尝试 update-by-file 重索引 2 个错误 PDF，均被 API 拒绝；改用删除错误记录后重建。
- 已重建 Altivar58 与 SINUMERIK 两份 PDF，当前处于 parsing，等待索引终态。
- 已确认 Embedding 服务恢复可用；CNC 探针上传成功，两份 PDF 正在进行大规模分段向量化。
- 77 个 Markdown 请求虽成功，但因同名覆盖实际只保留最后的 KB-11 分类；已停止继续写入并等待唯一命名方案确认。
- 1MB8014 PDF 超过 API 默认 15MB；已确认文件有效，等待选择提高限制或拆分。
- 用户已批准唯一命名、50MB 上传限制和分批修复方案；设计已写入 `docs/superpowers/specs/2026-07-21-dify-knowledge-repair-design.md`。
- 已配置 50MB 上传限制；API 容器正在等待迁移检查完成，Worker 保持运行。
- API 迁移检查仍在高 CPU 运行；已确认数据库可连接且未耗尽总连接，继续等待自然完成。
- API 已恢复健康且 50MB 限制生效；已清理 9 条冲突/错误记录并上传第 01 类。
- 已创建并通过 AST 语法检查的可恢复同步脚本，准备在容器内连续执行剩余修复。
- 首次同步完成分类 01-04 和分类 05 的 6 个文档；因单文档瞬时 I/O/Redis 超时停止，准备提高有限重试次数后续跑。
- 第二次续跑确认已完成项均可跳过；修复 DELETE 超时后 404 的幂等处理。
- 已完成分类 01-07，第 08 类已提交；正在降低轮询 API 压力后继续。
